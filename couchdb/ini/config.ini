{{ $couchdbConfig := dict "couchdb" (dict "uuid" (include "couchdb.uuid" .)) "jwt_keys" (dict "hmac:_default" .Values.jwtAuthSecret ) -}}
{{- $couchdbConfig := merge $couchdbConfig .Values.couchdbConfig -}}
{{- range $section, $settings := $couchdbConfig -}}
[{{ $section }}]
{{ range $key, $value := $settings -}}
{{ $key }} = {{ ($value | toString)}}
{{ end }}
{{ end }}
